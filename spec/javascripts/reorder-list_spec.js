/*jslint browser: true, sloppy: true, todo: true, devel: true */
/*global $, it, describe, xit, xdescribe, expect, beforeEach, spyOn, CustomFeedback */

//= require reorder-list

describe('ReorderableList', function () {
    var container;

    beforeEach(function () {
        // TODO: Fixtures or something to call it with
        container = new ReorderableList();
    });

    // TODO: Attributes
});

