import * as React from "react";

interface Props {
}

export const ManagedInteractiveAdmin: React.FC<Props> = () => {
  return (
    <div>This is the ManagedInteractiveAdmin React component placeholder.</div>
  );
};
