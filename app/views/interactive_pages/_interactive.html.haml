.interactive-mod{class: (layout == 'l-full-width') ? nil : 'pinned'}
  - if page.visible_interactives.first
    = render partial: "#{page.visible_interactives.first.class.name.underscore.pluralize}/show", locals: {interactive: page.visible_interactives.first}
