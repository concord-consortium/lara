.about
  %span.close
    close (✖)
  %h3
    About the #{@project.title} Project
  %p.title
    #{@project.title}
  .about-content
    = raw @project.about

:scss
  p.title,.about-content {
    padding: 0 5px;
  }
  h3 {
    font-size: 12pt;
    padding-top: 4px;
    padding-left: 5px;
    color: #000000;
    background: #CCCCCC;
    height:25px;
  }
  p.title,h3 {
    margin-bottom: 1em;
    font-weight: bold;
  }
  .close {
    position: absolute;
    top: 5px;
    right: 5px;
  }
