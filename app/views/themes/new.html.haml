= content_for :session do
  #session
    = render :partial => 'shared/session'
= content_for :title do
  Themes
= content_for :nav do
  .breadcrumbs
    %ul
      %li= link_to "Home", root_path
      %li 
        \/
        = link_to 'All Themes', themes_path
      %li / New Theme

%h1.title New Theme

=render :partial => 'form'